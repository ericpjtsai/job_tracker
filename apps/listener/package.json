{
  "name": "listener",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "sh -c 'set -a && . ./.env.local 2>/dev/null; set +a; tsx watch src/index.ts'",
    "start": "sh -c 'set -a && . ./.env.local 2>/dev/null; set +a; tsx src/index.ts'",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@job-tracker/scoring": "*",
    "@supabase/supabase-js": "^2.43.0",
    "eventsource": "^2.0.2",
    "linkedin-jobs-api": "^1.0.7",
    "node-html-parser": "^7.1.0"
  },
  "devDependencies": {
    "@types/eventsource": "^1.1.15",
    "@types/node": "^20.0.0",
    "tsx": "^4.9.0",
    "typescript": "^5.4.0"
  }
}
