Show job application stats and trends.

Query the Supabase database for:
1. Total jobs in DB, total applied, total by status
2. Applications in the past 7 days grouped by day
3. Top 10 companies applied to
4. Resume fit distribution (high/medium/low)
5. Jobs per source (firehose_rule column)

Use the Supabase credentials from `.env.local` or `apps/listener/.env.local`. Present results in a clean table format.
